<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techno.timeentry">

    <application
        android:name=".application.TimeEntryApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.loginactivity.LoginActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.mainactivity.MainActivity"
            android:theme="@style/AppThemeNoActionBar"
            android:screenOrientation="portrait"/>

        <activity android:name=".activities.timeentryformactivity.TimeEntryFormActivity"
            android:screenOrientation="portrait"
            android:label="@string/text_enter_details"
            android:parentActivityName=".activities.mainactivity.MainActivity"/>

    </application>

</manifest>